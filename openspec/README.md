# OpenSpec

This repository keeps aiflow-compatible change records under `openspec/changes/`.

Rules:

- One directory represents one business change.
- Role outputs are files under that change directory.
- Release and archive records must be explicit.
- Historical debt should be recorded as follow-up work instead of blocking unrelated changes.
