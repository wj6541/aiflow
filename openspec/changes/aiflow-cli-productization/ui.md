# UI: aiflow CLI Productization

ui_source: none
ui_target: product_usability

routes:
  - path: none

viewports:
  - desktop
  - tablet
  - mobile

## Notes

This change is CLI and documentation focused. UI verification behavior is covered through the static example app and command tests rather than a product UI surface.
