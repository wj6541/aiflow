# Monorepo Example

`aiflow doctor` detects workspace package managers, existing scripts, and workspace package roots.

`aiflow check` reports touched packages for the current diff:

```text
✓ Touched packages: packages/api
```

Package-specific command routing is intentionally project-configured in the first version.
